apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "world-healines.fullname" . }}-test-connection"
  labels:
    {{- include "world-healines.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "world-healines.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
