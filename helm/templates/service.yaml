apiVersion: v1
kind: Service
metadata:
  name: {{ include "world-healines.fullname" . }}
  labels:
    {{- include "world-healines.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "world-healines.selectorLabels" . | nindent 4 }}
