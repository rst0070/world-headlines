
export const environment = {
    backendApiUrl: process.env.BACKEND_API_URL || 'https://localhost',
    siteUrlPrefix: process.env.SITE_URL_PREFIX || 'http://localhost:3000'
    // Add other environment configurations as needed
};